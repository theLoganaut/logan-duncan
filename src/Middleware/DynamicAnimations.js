import { keyframes } from "styled-components";

export const one = keyframes`
    from {
      height: 0px;
    }

    to {
      height: 33px;
    }
  `;

export const two = keyframes`
  from {
    height: 0px;
  }

  to {
    height: 74px;
  }
`;

export const three = keyframes`
    from {
      height: 0px;
    }

    to {
      height: 115px;
    }
  `;

export const four = keyframes`
    from {
      height: 0px;
    }

    to {
      height: 115px;
    }
  `;

export const five = keyframes`
    from {
      height: 0px;
    }

    to {
      height: 156px;
    }
  `;
